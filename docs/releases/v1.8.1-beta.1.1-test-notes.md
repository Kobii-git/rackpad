# Rackpad v1.8.1-beta.1.1 release test notes

## Status

Recovery candidate in progress on 2026-08-30. This is not a supported stable
release. Experimental installation instructions remain withheld until the
formal prerelease exists and a fresh Debian 13 smoke test passes.

## Recovery intent

- Keep the failed `v1.8.1-beta.1` tag immutable.
- Wait for the duplicate-MAC filtered table state before reading its UI count.
- Publish source and GHCR artifacts only through the release workflow after its
  quality job passes.
- Use this release as the retained Phase 5 `beta.1.1 -> beta.2` update baseline.

## Publication evidence

Record the exact beta branch commit and workflow URLs after all branch gates
pass. After tagging, record the tag workflow, GitHub prerelease, source archive,
GHCR image, package version, tagged helper origin, and Community core pin.

Local candidate validation completed on 2026-08-30:

- The repaired duplicate-MAC scenario passed five consecutive targeted runs.
- `npm run check:full` passed with 253 server assertions, 41 client tests, and
  all 29 Playwright scenarios.
- `check:proxmox` passed its 16-asset contract and 12 isolated fixtures.
- Actionlint, Bash syntax, ShellCheck, the production npm audit, and Trivy
  high/critical vulnerability, secret, and misconfiguration scans passed.
- PowerShell was unavailable in the local environment; the exact-commit GitHub
  quality workflow must supply that parser evidence before tagging.

## Debian 13 smoke before tester instructions

- Proxmox VE 9.x `amd64`, unprivileged LXC, nesting enabled, 2 vCPU, 4 GB RAM,
  16 GB disk, DHCP, and TCP 3000.
- Successful native build, service activation, health checks, first-admin
  bootstrap, and reboot persistence.
- Root-owned read-only code, `rackpad`-owned persistent data, hardened systemd
  service, and no leaked secret values.
- Backup, offline restore, and password reset.
- Safe discovery; advanced discovery success or truthful refusal.
- UDP 1162 closed by default and Docker Compose collision refusal.
- Custom-port operation through `/etc/rackpad/rackpad.env`.

## Ubuntu 24.04 Phase 4 completion

Repeat the critical fresh-install and operational checks on Ubuntu 24.04. Keep
both validated guests for the Beta 2 update/rollback phase. Phase 4 remains
incomplete until both guests pass without unresolved release, data-loss,
privilege, or installation defects.
